#include <stdio.h>
int main() {
int length = 12;
int breadth = 16;
int area;
area = length * breadth;
printf("Length = %d\n", length);
printf("Breadth = %d\n", breadth);
printf("Area of rectangle = %d\n", area);
return 0;
}
